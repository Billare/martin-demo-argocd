apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: martin-argocd-demo
  labels:
    app: martin-argocd-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: martin-argocd-demo
